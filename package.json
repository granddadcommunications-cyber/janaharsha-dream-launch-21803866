{
  "name": "janaharsha-plots-static",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "preview": "npx serve dist",
    "format": "prettier --write .",
    "lint": "node scripts/build-static.mjs --check"
  },
  "devDependencies": {
    "prettier": "^3.7.3"
  }
}
